name: Simple Video Hosting - Web Server

on:
  push:
    branches: [ main ]
    paths:
    - 'web/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Login to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build image
      uses: docker/build-push-action@v5
      with:
        context: ./web
        push: true
        platforms: linux/amd64
        cache-from: type=registry,ref=ghcr.io/sverdlovsky/svh-web:cache
        cache-to: type=registry,ref=ghcr.io/sverdlovsky/svh-web:cache,mode=max
        tags: |
          ghcr.io/sverdlovsky/svh-web:latest
          ghcr.io/sverdlovsky/svh-web:0.1.0

